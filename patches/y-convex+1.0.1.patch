diff --git a/node_modules/y-convex/dist/component/lib.js b/node_modules/y-convex/dist/component/lib.js
index dffe6e7..41d3e0f 100644
--- a/node_modules/y-convex/dist/component/lib.js
+++ b/node_modules/y-convex/dist/component/lib.js
@@ -35,7 +35,8 @@ export const push = mutation({
     returns: v.null(),
     handler: async (ctx, args) => {
         const decoded = Y.decodeUpdate(new Uint8Array(args.update));
-        if (decoded.structs.length) {
+        const hasDeletes = decoded.ds.clients.size > 0;
+        if (decoded.structs.length || hasDeletes) {
             await ctx.db.insert("update", args);
         }
     },
